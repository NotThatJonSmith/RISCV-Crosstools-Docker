cd gcc
./contrib/download_prerequisites


mkdir build/cross-gcc-build
mkdir build/cross-gcc-install

cd build/cross-gcc-install
../../external/gcc/configure \
--host=`gcc -dumpmachine` \
--target=riscv32-linux-eabi \
--with-arch=rv32gc \
--prefix=`realpath ../../cross-gcc-install` \
--enable-languages=c,c++

make

